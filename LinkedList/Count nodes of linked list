/*
Count the number of nodes 
in the linked list
Node is defined as 
struct node
{
    int data;
    struct node* next;
};
 
*/

int getCount(struct node* head)
{
 struct node *p;
 int c=0;
 p = head;
 while(p!=NULL)
 {
     c++;
     p=p->next;
 }
 return c;
}
